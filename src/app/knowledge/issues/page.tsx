import{issues}from'@/data/mock';import{Card,PageHeader,StatusBadge}from'@/components/ui';export default function Issues(){return <><PageHeader title='Known Issues' desc='Filter by open, resolved, critical, production and application.'/><div className='grid-auto'>{issues.map(i=><Card key={i.id}><h2>{i.title}</h2><p>Severity: <StatusBadge>{i.severity}</StatusBadge></p><p>Status: {i.status}</p><p>Impact: {i.impact}</p><p>Workaround: {i.workaround}</p><p>Owner: {i.owner}</p></Card>)}</div></>}
