'use client';import Link from'next/link';import{useState}from'react';import{articles}from'@/data/mock';import{Card,PageHeader,SearchInput,StatusBadge}from'@/components/ui';export default function K(){const[q,setQ]=useState('');return <><PageHeader title='Knowledge Base' desc='Architecture, business, database, deployment, troubleshooting, legacy, production support and FAQ.'/><SearchInput value={q} onChange={setQ}/><div className='grid-auto mt-4'>{articles.filter(a=>a.title.toLowerCase().includes(q.toLowerCase())).map(a=><Card key={a.id}><h2>{a.title}</h2><StatusBadge>{a.category}</StatusBadge><p>Application: {a.application}</p><p>Owner: {a.owner}</p><p>Updated: {a.updated}</p><Link className='btn' href={`/knowledge/${a.id}`}>Read Article</Link></Card>)}</div></>}
