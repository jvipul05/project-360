'use client';import Link from'next/link';import{useState}from'react';import{users}from'@/data/mock';import{Card,PageHeader,SearchInput,StatusBadge}from'@/components/ui';export default function People(){const[q,setQ]=useState('');return <><PageHeader title='People Directory' desc='Find owners, SPOCs and expertise.'/><SearchInput value={q} onChange={setQ}/><div className='grid-auto mt-4'>{users.filter(u=>u.name.toLowerCase().includes(q.toLowerCase())).map(u=><Card key={u.id}><h2><Link href={`/people/${u.id}`}>{u.name}</Link></h2><p>{u.role}</p><p>Applications: {u.applications.join(', ')}</p><p>Expertise: {u.expertise.map(e=><StatusBadge key={e}>{e}</StatusBadge>)}</p><p>Availability: {u.availability}</p></Card>)}</div></>}
