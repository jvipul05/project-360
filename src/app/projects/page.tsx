'use client';import Link from'next/link';import{useState}from'react';import{projects}from'@/data/mock';import{Card,PageHeader,ProgressBar,SearchInput,StatusBadge}from'@/components/ui';export default function Projects(){const[q,setQ]=useState('');return <><PageHeader title='Projects' desc='Search, filter and open enterprise projects.' action={<button className='btn'>Create Project</button>}/><SearchInput value={q} onChange={setQ}/><div className='grid-auto mt-4'>{projects.filter(p=>p.name.toLowerCase().includes(q.toLowerCase())).map(p=><Card key={p.id}><h2 className='text-xl font-bold'>{p.name}</h2><p>{p.code}</p><p>{p.applications} Applications · {p.members} Members</p><p>Project Manager: <b>{p.manager}</b></p><p>Technical Manager: <b>{p.technicalManager}</b></p><StatusBadge>{p.status}</StatusBadge><ProgressBar value={p.readiness}/><Link className='btn mt-4' href={`/projects/${p.id}`}>Open Project</Link></Card>)}</div></>}
