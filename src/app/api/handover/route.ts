import { NextResponse } from 'next/server';import { handovers } from '@/data/mock';export async function GET(){return NextResponse.json(handovers)}export async function POST(request:Request){const body=await request.json();return NextResponse.json({...body,id:`ho-${Date.now()}`})}
