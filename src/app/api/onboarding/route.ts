import { NextResponse } from 'next/server';import { onboardingPlans } from '@/data/mock';export async function GET(){return NextResponse.json(onboardingPlans)}export async function POST(request:Request){const body=await request.json();return NextResponse.json({...body,id:`onb-${Date.now()}`})}
