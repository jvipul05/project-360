'use client';import Link from'next/link';import{pct,useApp}from'@/components/state';import{Card,PageHeader,ProgressBar}from'@/components/ui';export default function Handover(){const{hos}=useApp();return <><PageHeader title='Handover' desc='Outgoing to incoming ownership transfer.' action={<button className='btn'>Create Handover</button>}/><div className='grid-auto'>{[['Active',4],['Completed',21],['At Risk',2],['Average Completion','76%']].map(x=><Card key={x[0]}><p>{x[0]}</p><h2>{x[1]}</h2></Card>)}</div><div className='grid-auto mt-4'>{hos.map((h:any)=><Card key={h.id}><h2>{h.outgoing}<br/>↓<br/>{h.incoming}</h2><p>Application: {h.application}</p><ProgressBar value={pct(h.items)}/><p>Deadline: {h.deadline}</p><p>Risk: {h.risk}</p><Link className='btn' href={`/handover/${h.id}`}>Open Handover</Link></Card>)}</div></>}
