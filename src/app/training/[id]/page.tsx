'use client';import{useApp}from'@/components/state';import{Card,PageHeader}from'@/components/ui';export default function T({params}:any){const{trs,markTrainingCompleted}=useApp();const t=trs.find((x:any)=>x.id===params.id)||trs[0];return <><PageHeader title={t.topic} desc={`${t.trainer} → ${t.trainee} · ${t.date} · ${t.duration} minutes · ${t.status}`}/><Card>{['Agenda','Materials','Notes','Recording','Attendance','Assessment'].map(s=><p key={s}><b>{s}</b>: {s==='Agenda'?t.agenda.join(', '):'Prepared for session.'}</p>)}<button className='btn' onClick={()=>markTrainingCompleted(t.id)}>Mark Completed</button></Card></>}
